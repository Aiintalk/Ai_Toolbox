import { chatStream } from '@/lib/yunwu';
import { NextRequest } from 'next/server';

export const runtime = 'nodejs';
export const maxDuration = 120;

const SYSTEM_PROMPT = `你是一个专业的抖音账号对标分析师。用户会提供一个抖音账号的内容数据，你需要根据这些数据生成两份分析文档。

用户会提供两组数据：
1. 全账号点赞TOP10的视频文案（代表这个账号历史上最能打的内容）
2. 最近30天的全部视频文案（代表当前内容策略和方向）

你需要输出两份文档，用 === 分隔符分开：

第一份：【人格档案】
参考以下结构输出（根据实际数据灵活调整，不要生硬套模板）：

账号名 · 人格档案

基本信息
- 从内容中推断出的身份信息（年龄、城市、职业、家庭等）
- 账号基本数据（粉丝量级、账号名等，如用户提供了的话）

人生经历（如果内容中能提取到）
- 从视频内容中提取的关键人生经历、转折点
- 按时间线或重要性排列

说话风格
语气特征：
- 从文案中提炼出的语气特点（3-5个，每个带具体例句）

常用句式：
- 从文案中高频出现的句式模式（5-8个，直接引用原文）

禁用表达（如果能观察到的话）：
- 这个人明显不会用的表达方式

人设内核
- 一句话总结这个人的核心人设
- 权威性来源是什么（经验型？专业型？共情型？）
- 用户为什么信任她/他

内容品味
- 她/他喜欢什么类型的表达
- 她/他不喜欢什么类型的表达

===SPLIT===

第二份：【内容规划】
参考以下结构输出：

账号名 · 内容规划方案

一、人设定位
一句话定位
关键词拆解（3-5个关键词，每个说明内涵和用户感知）
凭什么听她/他的——核心支撑点

二、内容体系
用树状图展示内容分类：
- 有哪些固定栏目/系列
- 各栏目占比大概多少
- 纯内容 vs 带货内容的比例

对每个栏目/系列详细说明：
- 定位和作用
- 内容公式（结构拆解）
- 典型选题（从实际内容中提取）

三、爆款规律
- TOP10内容的共性分析
- 开头钩子的常用模式
- 什么类型的选题容易爆

四、更新频率
- 每周大概更新几条
- 各类型内容的更新节奏

注意事项：
- 所有分析必须基于用户提供的实际文案，不要编造
- 引用具体文案作为证据
- 如果某个维度数据不足无法分析，坦诚说明，不要强行填充
- 语气客观专业，像一个资深内容策划在做竞品分析报告`;

export async function POST(req: NextRequest) {
  const { accountName, top10Content, recent30Content } = await req.json();

  const userMessage = `请分析以下抖音账号：${accountName || '（未提供账号名）'}

## 全账号点赞TOP10视频文案

${top10Content || '（未提供）'}

## 最近30天全部视频文案

${recent30Content || '（未提供）'}

请根据以上内容，输出【人格档案】和【内容规划】两份文档，用 ===SPLIT=== 分隔。`;

  const stream = chatStream(
    [{ role: 'user', content: userMessage }],
    SYSTEM_PROMPT
  );

  return new Response(stream, {
    headers: {
      'Content-Type': 'text/plain; charset=utf-8',
      'Transfer-Encoding': 'chunked',
    },
  });
}
